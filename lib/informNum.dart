import 'package:flutter/material.dart';

class informNum extends StatelessWidget {
  const informNum({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Information Number'),
    );
  }
}
