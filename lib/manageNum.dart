import 'package:flutter/material.dart';

class manageNum extends StatelessWidget {
  const manageNum({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('manage Number'),
    );
  }
}
