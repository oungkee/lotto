import 'package:flutter/material.dart';

class rank extends StatelessWidget {
  const rank({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('rank information'),
    );
  }
}
